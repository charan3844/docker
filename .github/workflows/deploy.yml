name: Team B – Build & Deploy

on:
  push:
    branches: [release]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    uses: ./.github/workflows/reusable-docker-aca.yml
    with:
      environment: prod
      image_tag: latest
    secrets: inherit
