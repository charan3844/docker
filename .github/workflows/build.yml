name: Team A – Build & Deploy

on:
    push:
        branches: [main]
    workflow_dispatch:

permissions:
    contents: read
    id-token: write

jobs:
    deploy:
        uses: ./.github/workflows/reusable-docker-aca.yml
        with:
            environment: dev
            image_tag: latest
        secrets: inherit
